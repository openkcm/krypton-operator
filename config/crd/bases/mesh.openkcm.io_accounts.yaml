---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: accounts.mesh.openkcm.io
spec:
  group: mesh.openkcm.io
  names:
    kind: Account
    listKind: AccountList
    plural: accounts
    shortNames:
    - acct
    singular: account
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Account is the Schema for the accounts API.
          An Account can own multiple CryptoEdgeDeployments.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AccountSpec defines the desired state of an Account.
            properties:
              displayName:
                description: DisplayName is a human-readable name for this account.
                type: string
              owner:
                description: Owner is the entity responsible for this account (e.g.,
                  customer name, org).
                type: string
            type: object
          status:
            description: AccountStatus captures the observed state.
            properties:
              lastSyncTime:
                description: LastSyncTime records when the account was last reconciled.
                format: date-time
                type: string
              ready:
                description: Ready indicates if the account is in operational state.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
